language: objective-c
osx_image: xcode8
rvm:
 - 2.3.0

before_install:
    - bundle install
    - bundle exec pod install

script:
 - xcodebuild build-for-testing test-without-building -scheme KeyedMapper -workspace KeyedMapper.xcworkspace -sdk iphonesimulator -destination "name=iPhone SE" | xcpretty
